<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <script src="https://cdn.tailwindcss.com"></script>
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300&display=swap" rel="stylesheet">
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
        {% endblock %}
    </head>
    <body>
        <header class="bg-blue-600 p-4 text-white">
            <div class="container mx-auto flex justify-between items-center">
                <a href="{{ path('app_home') }}">
                    <h1 class="text-2xl font-semibold uppercase">
                        Localist
                    </h1>
                </a>

                {% if app.user %}
                    <a href="{{ path('app_dashboard') }}" class="bg-green-500 px-4 py-2 rounded text-white hover:bg-green-600">
                        Dashboard
                    </a>
                {% else %}
                    <a href="{{ path('app_login') }}" class="bg-yellow-500 px-4 py-2 rounded text-white hover:bg-yellow-600">
                        Je suis commerçant
                    </a>
                {% endif %}
            </div>
        </header>
        {% block body %}{% endblock %}
    </body>
</html>
