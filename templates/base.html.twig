<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <script src="https://cdn.tailwindcss.com"></script>
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300&display=swap" rel="stylesheet">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" integrity="sha512-z3gLpd7yknf1YoNbCzqRKc4qyor8gaKU1qmn+CShxbuBusANI9QpRohGBreCFkKxLhei6S9CQXFEbbKuqLg0DA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
        {% block stylesheets %}
            <style>
                .w-full::-webkit-scrollbar {
                    display: none !important;
                }
                .w-full::-webkit-scrollbar-thumb {
                    background-color: darkgrey;
                    outline: 1px solid slategrey;
                    border-radius: 5px; 
                }
                .w-full::-webkit-scrollbar-track {
                    background: lightgrey;
                }
            </style>
        {% endblock %}

    </head>
    <body class="bg-gray-100">
        <header class="p-3 sticky top-0 z-50">
            <div class="bg-green-200 p-4 rounded-full container mx-auto flex justify-between items-center">
                <a href="{{ path('app_home') }}">
                    <h1 class="text-2xl text-gray-700 font-semibold uppercase">
                        Localist
                    </h1>
                </a>
                
                {% if app.user %}
                    <div>
                    <a href="{{ path('app_dashboard') }}" class="bg-gray-500 px-4 py-2 rounded-full text-white hover:bg-gray-600">
                        <i class="fa-solid fa-house"></i> Dashboard
                    </a>
                    <a href="{{ path('app_logout') }}"><i class=" ml-2 fa-solid fa-right-from-bracket" ></i></a>
                    </div>
                {% else %}
                    <a href="{{ path('app_login') }}" class="bg-gray-500 px-4 py-2 rounded-full text-white hover:bg-gray-800">
                        <i class="fa-solid fa-house"></i> Je suis commerçant
                    </a>
                {% endif %}

                
            </div>
        </header>
        </div>
        {% block body %}{% endblock %}
    </body>
    {% block javascripts %}
    {% endblock %}
</html>
