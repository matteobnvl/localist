{% extends 'base.html.twig' %}

{% block title %}New Post{% endblock %}

{% block body %}
    <div class="m-4 flex">
        <a href="{{ path('app_shop_keeper_show', {'id': shop_keeper.id}) }}" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline mr-3">retour</a>
    </div>
    <div class="flex items-center justify-center">
        <h1 class="text-3xl font-bold text-gray-900">
            Ajouter un post
        </h1>
    </div>
    <div class="w-1/3 mx-auto">
        {{ include('post/_form.html.twig') }}
    </div>
{% endblock %}
