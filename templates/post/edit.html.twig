{% extends 'base.html.twig' %}

{% block title %}Edit Post{% endblock %}

{% block body %}
    <div class="m-4 flex">
        <a href="{{ path('app_shop_keeper_show', {'id': post.postedBy.id}) }}" class="bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded-full focus:outline-none focus:shadow-outline mr-3"><i class="fa-solid fa-arrow-left"></i> retour</a>
        {{ include('shop_keeper/_delete_form.html.twig') }}
    </div>
    <div class="flex items-center justify-center">
        <h1 class="text-3xl font-bold text-gray-900">
            Modifier un post
        </h1>
    </div>
    <div class="w-1/3 mx-auto">
        {{ include('post/_form.html.twig', {'button_label': 'Modifier'}) }}
    </div>
{% endblock %}
