<form method="post" action="{{ path('app_shop_keeper_delete', {'id': shop_keeper.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ shop_keeper.id) }}">
    <button class="class=bg-blue-500 px-4 py-2 rounded text-white hover:bg-green-600">Delete</button>
</form>
