{% extends 'base.html.twig' %}

{% block title %}New Notice{% endblock %}

{% block body %}
    <div class="m-4">
        <a href="{{ path('app_shop_keeper_show', {'id': shop_keeper.id}) }}" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline">retour</a>
    </div>
    <div class="flex items-center justify-center">
        <h1 class="text-3xl font-bold text-gray-900">
            Ajouter votre avis
        </h1>
    </div>
    <div class="mx-auto w-1/3">
        {{ include('notice/_form.html.twig') }}
    </div>
{% endblock %}
